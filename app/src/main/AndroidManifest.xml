<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pcland15.ismail.gtc_t_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/gtc">
        <activity android:name="tabuk.amin.e.gtc_t_app.login"></activity>
        <activity android:name="tabuk.amin.e.gtc_t_app.items_categories" />
        <activity android:name="tabuk.amin.e.gtc_t_app.users_add" />
        <activity android:name="tabuk.amin.e.gtc_t_app.new_res_item" />
        <activity android:name="tabuk.amin.e.gtc_t_app.item_data" />
        <activity android:name="tabuk.amin.e.gtc_t_app.res_item"></activity>
        <activity android:name="tabuk.amin.e.gtc_t_app.users_items" />
        <activity android:name="tabuk.amin.e.gtc_t_app.home"></activity>
        <activity android:name="tabuk.amin.e.gtc_t_app.res_data"></activity>




        <activity android:name="tabuk.amin.e.gtc_t_app.splesh">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="tabuk.amin.e.gtc_t_app.item_map"
            android:label="@string/title_activity_item_map"></activity>
        <activity android:name="tabuk.amin.e.gtc_t_app.about"></activity>
        <activity android:name="tabuk.amin.e.gtc_t_app.test"></activity>
    </application>


</manifest>
